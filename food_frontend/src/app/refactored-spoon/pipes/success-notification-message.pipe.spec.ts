import { SuccessNotificationMessagePipe } from './success-notification-message.pipe';

describe('SuccessNotificationMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new SuccessNotificationMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
